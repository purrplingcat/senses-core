/**
 * Creates application runtime and executes their main function
 * @param manifestFile Path to application manifest
 * @param entry Path to applicaton entry file with the main function
 */
function runtime(manifestFile: string, entry?: string): void;

export default runtime;
