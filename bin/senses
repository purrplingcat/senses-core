#! /usr/bin/env node
/* eslint-disable @typescript-eslint/no-var-requires */
"use strict";
if (require.main === module) {
    const runtime = require("../runtime/node-app");
    const manifest = require("../manifest.json");

    runtime(manifest);
}
